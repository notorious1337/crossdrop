# WebSocket URL of the signaling server.
# Local dev default (used automatically if unset): ws://localhost:8787
# Production example: wss://signal.crossdrop.app
PUBLIC_SIGNALING_URL=ws://localhost:8787
