{
  "name": "crossdrop-signaling",
  "version": "1.0.0",
  "description": "Minimal WebSocket signaling server for CrossDrop. Relays WebRTC SDP/ICE only — never file data.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
